# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# SMTP (server-only — email sign-in codes via nodemailer)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=ynrdevs@gmail.com
SMTP_PASS=your-smtp-app-password
SMTP_FROM=RoadRunners <ynrdevs@gmail.com>

# Chikky AI (server-only — never NEXT_PUBLIC_)
CHIKKY_AI_API_KEY=your-api-key
CHIKKY_AI_MODEL=gemini-2.0-flash
CHIKKY_AI_PROVIDER=gemini
# CHIKKY_AI_PROVIDER=openai
# CHIKKY_AI_MODEL=gpt-4.1-mini

# ── Scrim runtime ─────────────────────────────────────────────
# browser = Sandpack/Pyodide only (default, no credits)
# daytona = server sandboxes for python (+ future go/shell)
# auto    = browser for react/vanilla, daytona for python
SCRIM_RUNNER=auto
SCRIM_TTS_ENABLED=true
SCRIM_MAX_CHECKPOINTS=5
SCRIM_MAX_USER_SCRIMS_PER_JOURNEY=20

# ── Daytona (server-only — code execution sandboxes) ──────────
# https://app.daytona.io → Settings → API Keys
# DAYTONA_TARGET must be a target enabled for your organization.
# If "us" is unavailable, choose an allowed target from Daytona or use SCRIM_RUNNER=browser.
DAYTONA_API_KEY=
DAYTONA_API_URL=https://app.daytona.io/api
DAYTONA_TARGET=us
DAYTONA_DEFAULT_LANGUAGE=python
DAYTONA_SANDBOX_TTL_MINUTES=30

# ── ElevenLabs (server-only — scrim caption narration) ────────
# https://elevenlabs.io/app/settings/api-keys
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=JBFqnCBsd6RMkjVDRZzb
ELEVENLABS_MODEL_ID=eleven_multilingual_v2
ELEVENLABS_OUTPUT_FORMAT=mp3_44100_128

# Optional: Supabase Storage bucket for TTS cache (defaults to tts-cache)
TTS_STORAGE_BUCKET=tts-cache

# Optional: Cloudflare R2 TTS cache instead of Supabase Storage
# Dashboard -> R2 -> create bucket + R2 API token with Object Read & Write
# TTS_STORAGE_BACKEND=supabase keeps the default Supabase Storage path.
TTS_STORAGE_BACKEND=supabase
CLOUDFLARE_R2_ACCOUNT_ID=
CLOUDFLARE_R2_ACCESS_KEY_ID=
CLOUDFLARE_R2_SECRET_ACCESS_KEY=
CLOUDFLARE_R2_BUCKET=roadrunners-tts-cache

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# OAuth — show provider buttons on /login (credentials go in Supabase, not here)
NEXT_PUBLIC_AUTH_GOOGLE_ENABLED=true

# Google OAuth checklist:
# 1. Google Cloud Console → Credentials → OAuth 2.0 Client ID (Web application)
#    Authorized JavaScript origins: http://localhost:3000
#    Authorized redirect URI: https://YOUR_PROJECT.supabase.co/auth/v1/callback
# 2. Supabase → Auth → Providers → Google → Client ID + Client Secret
# 3. Supabase → Auth → URL Configuration → Redirect URLs:
#    http://localhost:3000/auth/callback
#    http://localhost:3000/auth/confirm
